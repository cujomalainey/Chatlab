function [success] = sendHandshakeChatBeginPacket(channel, tempKey, user, key)
%sendHandshakeChatPacket Create a handshake packet and send it to the channel
	packet = struct(	'Type', 'ChatShakeBegin',...
						'User', user,...
						'Key', tempKey...
						);
	success = sendMessage(channel, packet, key);
end